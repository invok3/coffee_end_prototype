plan a single page app (html, css, javascript) for a coffeeshop, the app will actually represent a prototype of a mobile app, so so we are focusing on protrait mode only (no landscape/wide screen needed). the prototype will simply showcase the process of allowing a user pre-ordering one of several products from the coffeeshop (coffee, milkshake, juice, slush cocktail), the system only works on (pick up and pay on arrival) no delivery, no e- payment, but the whole purpose of the prototype is the pick-up position, the coffeshop serves at one site with 2 possibilites (incoming/outgoing) side of the road, it only sells for drivers, drivers (customers/users) like to take their order on the go so fast, so they want to decide which way they are approaching, so i prefer a simple map split instead of a radio button, hovering the way the driver choses. the map should not be that complecated, 1 street splitted in 2 svg images, this will make it easier to hover selected side of the street.